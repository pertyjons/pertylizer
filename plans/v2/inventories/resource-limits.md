# Real-Time Resource and Admission Inventory

| Field         | Value      |
|---------------|------------|
| Status        | Active     |
| Phase         | 00A        |
| Last reviewed | 2026-08-13 |

This ledger records every current fixed limit, truncation point, bounded queue, buffer capacity, and script budget. V2
must preserve, raise, remove, or expose each as an explicit admission rule with a structured diagnostic.

## Limit classes

- `Platform capability`
- `Configurable safety budget`
- `Lossy retention/presentation budget`
- `Warning threshold`
- `Implementation artifact to remove`
- `Unknown`

## Ledger

Entries use `LIMIT-NNNN` identifiers. Next free identifier: `LIMIT-0076`.

Pass 1 recorded the resource, its value, and its enforcement site for every constant its method reached. Pass 2 read the
enforcing code for the entries that gate Phase 0A, ran a second discovery method aimed at truncation sites rather than
constant names, and allocated identifiers for the two families pass 1 deliberately left unnumbered.
`Proposed V2 rule` remained blank throughout passes 1-2 because it is the output of ADR-0021, not an input to it.

**Pass 3 — the class-and-owner sweep — has now applied accepted ADR-0021 to all 74 entries that existed then.** Every entry carries a
terminal failure class, one of the seven configuration owners, a proposed V2 rule, and a diagnostic. **No entry is
`Unknown`**: the 19 that were have been resolved, and the resolutions are visible as rules rather than as a class name
alone.

**What `Classified` means here, and what it does not.** The [register vocabulary](README.md) defines it as required
fields *and* disposition filled with supporting evidence. The disposition is the V2 rule, and its supporting evidence is
an accepted decision — ADR-0021's class semantics and ownership axes, cited per entry — not a measurement. **No value in
this ledger has been measured.** A `Classified` row states what happens when the limit is exceeded and who owns the
number; it does not assert that the number is right. Setting numbers stays with each owner: P00A-T005 for `HostProfile`,
and the relevant contract or ADR for the other six.

**The `HostProfile` numbers now exist**, in the `Draft`
[host profile and render limits specification](../specs/spec-host-profile-and-render-limits.md). It maps every one of
the 28 settled `HostProfile`-owned entries below to a successor field, with `LIMIT-0014` pending a split and gives each a value with a stated basis — queried,
derived, V1 carry-over, or chosen. Exactly **one** of those values is derived from measurement — `predicted_quantum_cost_ratio` — with two more
*chosen and anchored on* EVD-0003; an earlier revision of this paragraph said three were derived, which the
specification's own basis vocabulary corrected. It also picks up `LIMIT-0031`, whose owner here is `N/A — removed` but whose disposition creates a
profile field. This ledger stays authoritative for what exists in V1 and who owns it; the specification is
authoritative for the value.

**ADR ownership.** The *truncation question* for every row in the silent-truncation register below is owned by
**ADR-0021**, including the ones whose subject matter is not rendering: `LIMIT-0067` truncates at project load, not in
the audio callback, but the Phase 0A exit gate is what forbids accepting an undiagnosed silent truncation as baseline,
so ADR-0021 must decide it. A row may carry a second ADR only for a *different* question about the same resource —
`LIMIT-0013` and `LIMIT-0020` keep ADR-0027 for what the taps are for and who owns them, which is not the same question
as what happens when they overflow. No row carries two ADRs for one question.

Passes 1-2 stated this ownership rule in the preamble but left `LIMIT-0013` and `LIMIT-0020` carrying ADR-0027 alone, so
two of the register's five rows contradicted it. Both now carry ADR-0021 as well.

**Configuration owner is the second axis**, recorded in its own column since pass 3. ADR-0021 separates a limit's
failure behavior from who owns its configuration, because this ledger contains budgets that are not render-preparation
inputs at all. The sweep's distribution over the closed set of seven:

| Owner                    | Entries | Reading                                                                            |
|--------------------------|--------:|------------------------------------------------------------------------------------|
| `HostProfile`            |      28 | Render preparation capacity — the only owner that participates in plan admission   |
| **Undecided**            |       1 | `LIMIT-0014` — one constant sizing a GUI ring and a protocol ring; needs splitting |
| Node contract            |      12 | Capacities intrinsic to one node's DSP, declared into the `ResourceReport`          |
| Protocol contract        |       7 | Reply, message, and per-client caps owned by the surface that serializes them       |
| `N/A — removed`          |       9 | Entries that cease to exist in V2                                                  |
| Application settings     |       7 | Editor and session budgets, unrelated to any render plan                            |
| Domain/format contract   |       6 | Bounds that are part of a value's representation or identity                        |
| Job policy               |       5 | Bounds of one long-running render or analysis job                                   |

Two thirds of the ledger is therefore **not** a `HostProfile` concern, which is the finding that justified splitting the
axes: a single-axis model would have put 46 of 75 entries in a render-preparation input they have nothing to do with.

**Pass 4 moved one entry across that boundary and left another undecided.** `LIMIT-0015` was `HostProfile` until its use sites were read; it is a
deferred-drop channel, so it joins `N/A — removed`. The distribution above is the post-audit one, and the register is
now **75** entries: pass 4 added `LIMIT-0075`.

**Pass 4 also produced two false findings, kept here because of how they failed.** It reported `MAX_NESTING_DEPTH`,
`MAX_SOURCE_LEN`, and `SCRIPT_MAX_PARAMS` as having no ledger entry, concluding that 74 was an undercount; they are
`LIMIT-0037`, `LIMIT-0038`, and `LIMIT-0040`, at exactly those citations, and the search had been for the *constant
identifiers* while those rows name their limits in prose. And it reported `LIMIT-0043` as a silent truncation on the
strength of a `.min()`, without reading `ExpansionBuffer::push` — which rejects and counts, making the `.min()` a
defensive no-op.

**Both failures are the one the audit was convened to correct.** Its subject was that passes 1-3 read definitions
rather than uses; it then asserted an absence from a name search and a behaviour from a single line, twice. External
review caught both. The transferable rule is narrower than "read the use sites": **a claim about what code does is not
finished until you have read what feeds it, and a claim that something is absent is not finished until you have
searched for it the way it would be written.**

### Render and host I/O

| ID | Resource/limit | Current value | Enforcement site | Overflow behavior | Limit class | Configuration owner | Proposed V2 rule | Diagnostic | Evidence | ADR | Status |
|------------|----------------------------------------------------|-------------------------------------:|--------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------|---|------------------|----------------------------------|----------|------------------------------|------------|
| LIMIT-0001 | Engine-wide audio block ceiling (`MAX_BLOCK_SIZE`) | 4096 frames | `crates/synth_core/src/audio/types.rs:87` | **Two different behaviors, both silent.** Audio *input*: `synth_engine.rs:4082-4086` caps `usable = stereo_samples.min(buf_cap)` with the comment "we silently cap to buffer size (avoids RT allocation)" — the block's input tail is dropped. Audio *output*: `voice.rs:1139` calls `mono_buffer.resize(samples)` every block, and `AudioBuffer::resize` is `Vec::resize` (`module_traits.rs:156`) — for a block above 4096 it **allocates on the audio thread**. Not reachable through the app's own configuration: the requested buffer size is capped at `BufferSize::VERY_LARGE` = 1024 (see `LIMIT-0057`), so the path is only reached if a host delivers a larger callback than requested. The RT-allocation regression test covers 1, 64, 256, and 1024 frames only (`rt_alloc_guard.rs:141`). | Platform capability | `HostProfile` | `maximum_block_size` is a `HostProfile` field established from queried device capability. A larger callback is the terminal stream-contract fault: silence, both carries invalidated, `needs_reprepare` published, nothing allocated. | Oversized-callback, discarded-input-frame, and silence-filled-frame counters plus `needs_reprepare`, all in the structured diagnostics report | ADR-0021 §3 | ADR-0001, ADR-0021, ADR-0022 | Classified |
| LIMIT-0002 | Audio-input scratch buffer | `MAX_BLOCK_SIZE * 2` interleaved f32 | `crates/synth_engine/src/synth_engine.rs:904` |  | Platform capability | `HostProfile` | Not an independent limit: the scratch is sized from `maximum_block_size` at preparation. Under ADR-0001 the renderer only ever processes whole quanta, so it cannot be overrun from inside. | N/A — unreachable once `LIMIT-0001`'s fault is enforced; its counters cover the only path in | ADR-0021 §1, §3 | ADR-0021 | Classified |
| LIMIT-0003 | Return-bus frame buffer (`MAX_FRAME`) | `MAX_BLOCK_SIZE * 2` | `crates/synth_engine/src/return_bus.rs:50` |  | Platform capability | `HostProfile` | As `LIMIT-0002`: sized from `maximum_block_size` at preparation, not a limit of its own. | N/A — same path as `LIMIT-0001` | ADR-0021 §1, §3 | ADR-0021 | Classified |
| LIMIT-0004 | Headless render sample-rate ceiling | 192 000 Hz, derived from `DeviceSampleRate::MAX_SUPPORTED` (was 384 000) | `crates/pertylizer/src/render/mod.rs:51`, enforced at `render/command.rs:139` | Rejected with `RenderError::InvalidSampleRate`. Before the fix a higher rate was accepted, and buffers sized from the engine ceiling silently delivered less than their parameters advertised | Configurable safety budget | `HostProfile` | The supported sample-rate range is a `HostProfile` field, and a job outside it is refused at admission. **Fixed in V1 ahead of V2**: pass 3 found the render command accepting 384 kHz against an engine ceiling of 192 kHz, and `MAX_RENDER_SAMPLE_RATE` is now *derived* from `DeviceSampleRate::MAX_SUPPORTED` rather than hand-copied, with a test pinning that it stays derived. The V2 rule is unchanged by the fix — the range is still a profile field — but the silent case is gone from V1 now rather than at cutover. | `RenderError::InvalidSampleRate` today; a job admission error naming the requested rate and the profile range under V2 | ADR-0021 §4; site read | ADR-0021 | Classified |
| LIMIT-0005 | Headless render tail ceiling | 30.0 s | `crates/pertylizer/src/render/mod.rs:43` |  | Configurable safety budget | Job policy | A bound of one render job, owned by the job contract ADR-0028 defers to its Phase 4 entry gate. | Job admission error naming the requested and permitted tail | ADR-0021 §1; ADR-0028 | ADR-0028 | Classified |
| LIMIT-0006 | Headless render output-size ceiling | 512 MiB | `crates/pertylizer/src/render/mod.rs:54` |  | Configurable safety budget | Job policy | As `LIMIT-0005`: an output-size bound of one job, refused at admission rather than truncated mid-render. | Job admission error naming the projected and permitted size | ADR-0021 §1; ADR-0028 | ADR-0028 | Classified |
| LIMIT-0007 | Offline arrangement render length | 300.0 s | `crates/pertylizer/src/audio/arrangement_render.rs:64` |  | Configurable safety budget | Job policy | As `LIMIT-0005`. V1 *clamps* the range with a warning; a job bound is a refusal, since a silently shortened render is a wrong measurement rather than a small one. | Job admission error; the current clamp-and-warn is withdrawn | ADR-0021 §2; ADR-0028 | ADR-0028 | Classified |
| LIMIT-0008 | Offline arrangement pre-roll | 30.0 s | `crates/pertylizer/src/audio/arrangement_render.rs:72` |  | Configurable safety budget | Job policy | Pre-roll is a job parameter with a bound, not a render capability. Notes older than the bound are still lost, so the job reports how much pre-roll it dropped. | Job diagnostic naming the requested and applied pre-roll | ADR-0021 §1; ADR-0028 | ADR-0028 | Classified |
| LIMIT-0009 | Offline render block size | 256 frames | `crates/pertylizer/src/audio/arrangement_render.rs:51` |  | Implementation artifact to remove | `N/A — removed` | ADR-0001 clause 2 forbids a second quantum: offline and live render in the same units. The constant ceases to exist when the V2 renderer lands. | N/A — the entry closes with the change | ADR-0001 clause 2 | ADR-0001 | Classified |
| LIMIT-0010 | Headless settle blocks before capture | 32 blocks | `crates/pertylizer/src/render/headless.rs:51` |  | Implementation artifact to remove | `N/A — removed` | A workaround for asynchronous command drain during load. V2 prepares a plan synchronously off the audio thread, so there is nothing to settle and no block count to guess. | N/A — the entry closes with the change | ADR-0021 §1; master plan Phase 1 | ADR-0001 | Classified |
| LIMIT-0011 | Draft-quality render sample rate | 22 050 Hz | `crates/synth_core/src/render.rs:34` |  | Configurable safety budget | Job policy | A quality-preset value, not a cap. It belongs to the job's quality profile, where a caller can see which preset produced a result. | The job receipt names the preset and its rate | ADR-0021 §1; ADR-0028 |  | Classified |

### Command, event, and telemetry queues

| ID | Resource/limit | Current value | Enforcement site | Overflow behavior | Limit class | Configuration owner | Proposed V2 rule | Diagnostic | Evidence | ADR | Status |
|------------|----------------------------------------------------|------------------------------:|---------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------|---|------------------|-----------------------------------------------------------------------------|----------|--------------------|---------------|
| LIMIT-0012 | Engine command ring (`CommandCapacity::DEFAULT`) | 16 384 commands | `crates/synth_engine/src/synth_engine.rs:49` | Overridable per engine via `SynthEngine::with_command_capacity`. Enqueue failures are counted by `CommandSync::dropped`, which makes `apply_project` fail a partly-received load. | Configurable safety budget | `HostProfile` | Real-time communication capacity is a `HostProfile` budget. Enqueue failure keeps counting and keeps failing a partly-received project load — the one place V1 already refuses rather than continuing. | `CommandSync::dropped` promoted from an internal counter to a field of the structured diagnostics report | ADR-0021 §1, §4 | ADR-0021, ADR-0035 | Classified |
| LIMIT-0013 | Prioritized event rings (critical/high/normal/low) | 256 / 256 / 512 / 2048 events | `crates/synth_engine/src/event_priority.rs:75-78` | **Corrected by the pass-4 use-site audit; passes 1-3 recorded this wrongly and ADR-0021 was accepted on the wrong version.** Per-priority `dropped_counts: [AtomicU32; 4]` exist, but `get_dropped_counts` (`event_priority.rs:194`) has **no caller anywhere**, so they are published nowhere — not on OSC, as the ledger and ADR-0021 both stated. OSC's `/synth/engine/event_drops` reads `EngineState::event_drops` (`state.rs:585`, incremented at `synth_engine.rs:2186,2261`), a different counter on a different ring. The module *documents* critical events as never dropped; `send` (`event_priority.rs:141-150`) uses the same one-shot `try_push` for every priority and counts the failure, so **critical is droppable too**. And the channel is never constructed (`lib.rs:58-59` is its only reference outside the module), so none of it runs. | Configurable safety budget | `HostProfile` | **Disposition withdrawn by the pass-4 use-site audit; awaiting an ADR-0021 clarification or successor.** ADR-0021 accepted it as "a genuinely live bounded queue" whose counters would be "promoted from OSC-only publication" — and both readings are false, so the disposition rests on evidence that does not exist. Nothing may be built on this row until the record is corrected. | To be decided with the ADR. The counters are published nowhere today, so there is no promotion to make — they have to be created | ADR-0021 §1, §3, both disproved by the use-site audit at `3555c52c` | ADR-0021, ADR-0027 | Needs review |
| LIMIT-0014 | `EVENT_BUFFER_SIZE` — **two egress rings with different destinations**: engine-to-GUI events, and OSC note telemetry | 256 each | `crates/synth_engine/src/synth_engine.rs:81`, used only at `:811` (`HeapRb::<EngineEvent>`, consumed by the GUI) and `:815` (`HeapRb::<NoteEvent>`, created "for OSC telemetry" at `:814` and handed to `synth_osc` from `pertylizer/src/main.rs:403-416`) | **Corrected by external review.** Passes 1-3 recorded this as the "engine event scratch buffer", a per-block renderer limit. It is not: both uses are engine **egress**, one to the GUI and one to OSC. What V1 actually fills per block is `sequencer_event_buffer: Vec<SequencerEvent>` (`synth_engine.rs:722`), a `Vec::with_capacity(128)` at `:895` refilled at `:4139-4141` — a capacity *hint* with no cap, like `LIMIT-0031`. **V1 has no per-quantum event limit.** **Overflow is not uniform, and the ledger must not flatten it — pass 4 did, and review corrected it.** Three behaviours share one ring. (a) Most `EngineEvent` producers discard the result — `let _ = ...try_push(...)` at `synth_engine.rs:2165`, `:2206`, `:2278`, `:2305`, `:4190`, `:4333`, and `metering.rs:82` — losing events with no counter. (b) **`RecordedNotesFlushed` is retried — but only once, and the backstop silently overwrites.** `flush_recorded_notes` (`synth_engine.rs:2386-2400`) matches the `Err` and parks the payload in `pending_recorded_notes`, retried at `:4089-4090`. The park is a bare `self.pending_recorded_notes = Some(...)` with **no `is_none()` guard** — unlike the mod-grid backstop at `:1783-1792` — so if the ring is still full at the next loop or stop flush, the second assignment overwrites the first, **losing recorded notes and dropping their `Vec` on the audio thread**. A GUI stall spanning two flushes is enough. (c) `NoteEvent` failures increment `EngineState::event_drops` (`:2175-2187`). **A single drop-counter policy over this ring would demote (b) to (a) and regress recorded-note delivery**, so the V2 rule must classify producers, not the ring. **Eighth entry in the silent-truncation register**, for the `EngineEvent` side. | Configurable safety budget | **Undecided — split pending** | Both are engine **egress**, but to different destinations: one to the GUI, one to OSC. They are a real bounded capacity and **not one owner** — a GUI event stream and a protocol telemetry stream can take different loss semantics, and ADR-0021 gives protocol caps their own owner. Splitting them is open work, so this row's owner is provisional. What does **not** follow from this entry is `max_events_per_quantum`: that field is *chosen*, not carried over, and the specification now labels it so. **The entry must be split before it can be classified**: one constant sizes two rings with different destinations, and ADR-0021 requires exactly one owner per entry, so a GUI stream and a protocol stream cannot share this row. | Drop counters for both rings in the structured diagnostics report; the `EngineEvent` side has none today | Use-site audit at `3555c52c`; ADR-0021 §1, §4 |  | Investigating |
| LIMIT-0015 | Deferred-drop return channels (`RETURN_BUFFER_SIZE`) | 256 slots, x4 channels | `crates/synth_engine/src/synth_engine.rs:84`, used at `:819`, `:823`, `:829`, `:856` | **Corrected by the pass-4 use-site audit.** Passes 1-3 recorded this as a "return scratch buffer" belonging to the bus and send budget. It is not: the constant sizes four `HeapRb` channels carrying `DroppedModule`, `Box<Instrument>`, `AutomationTarget`, and `Box<ModGridRuntime>` off the audio thread, which its own doc comment at `:83` states. It has nothing to do with return buses. **And its overflow is a real-time-safety issue, not just a lost message**: `let _ = self.instrument_return_producer.try_push(instrument)` (`synth_engine.rs:1988`) and `let _ = self.return_producer.try_push(DroppedModule(module))` (`:3165`) discard the `Err`, so when a channel is full the heap-owned value is **dropped on the audio thread** — the exact outcome the channel exists to prevent. Automation trash (`sequencer_engine.rs:696`) has the same shape. The mod-grid path has a backstop — `synth_engine.rs:1778-1790` parks a failed push in a single-slot `mod_grid_pending_drop` — but reaches `drop(back)` at `:1792` when the ring *and* the slot are both full. **Its comment calls that reachable "only if the main thread stopped draining entirely"; external review showed it is reachable from a delay.** 258 queued `SetModGrid` commands drained in one `process_commands` call fill the 256-slot ring, then the slot, and the 258th destroys a heap value on the audio thread. The main thread need only be late during the burst. Not yet fully audited; recorded so it is not lost. | Implementation artifact to remove | `N/A — removed` | The same mechanism as `LIMIT-0016`, and it takes the same disposition: V2 retires plans and node state through the prepared-plan retirement path, so the deferred-drop channels do not survive. **It is not a `HostProfile` field**, and the successor entry the specification carried for it is withdrawn. | N/A — the entry closes with the change, but its V1 overflow behaviour is only partly read | Use-site audit at `3555c52c`, **incomplete**; ADR-0021 §1 | ADR-0021 | Investigating |
| LIMIT-0075 | Per-block sequencer event buffer (`sequencer_event_buffer`) | `Vec::with_capacity(128)` — a hint, not a bound | `crates/synth_engine/src/synth_engine.rs:722`, allocated at `:895`, cleared and refilled at `:4139-4141` | **Audio-thread allocation.** `events.push(...)` at `sequencer_engine.rs:950`, `:970`, `:1031`, `:1055` grows the `Vec` when a block emits more than 128 sequencer events, and it runs inside `SynthEngine::process`. Nothing truncates and nothing is counted; the failure mode is a real-time violation rather than lost data, which is why neither discovery pass found it — pass 1 matched constant names and there is no constant, pass 2 matched truncation comments and nothing truncates. **Added by external review during the pass-4 use-site audit**, after that audit removed `LIMIT-0014`'s incorrect description, which had been the ledger's only gesture toward this buffer. | Implementation artifact to remove | `N/A — removed` | The same shape as `LIMIT-0031`: a capacity hint whose overrun allocates on the audio thread. V2 preallocates a declared per-quantum event capacity and admits against it, so the unbounded `Vec` ceases to exist. Until then it is a live V1 real-time hazard, not merely a migration item. **A V1 fix needs a decision, which is why one was not made:** (a) bound the push and count the drop, following `ExpansionBuffer`'s precedent in the same crate — needs a capacity number; (b) derive that capacity per song from track count x `MAX_EXPANSION_EVENTS_PER_TICK` and reserve it when the song changes — derived rather than invented, but only correct if the reserve happens off the audio thread; (c) leave it, on the grounds that `clear()` retains capacity so the allocation is a one-time cost per high-water mark rather than per block, and the hazard self-quiesces. (c) is the current behaviour stated honestly; it is still a violation. | N/A — the entry closes with the change; the V1 hazard has no diagnostic today | Use-site audit at `3555c52c`; `AGENTS.md` real-time rules | ADR-0021 | Classified |
| LIMIT-0016 | Script trash (deferred-drop) buffer | 2048 | `crates/synth_engine/src/synth_engine.rs:93` | Deferred-drop slots are sized to the ring capacity; a pointer-swap command waits for a free slot rather than dropping on the audio thread. | Implementation artifact to remove | `N/A — removed` | A V1 mechanism for dropping script state off the audio thread. V2 retires plans and node state through the prepared-plan retirement path, whose budget is a `HostProfile` field; the trash ring does not survive. | N/A — the entry closes with the change | ADR-0021 §4; ADR-0008 | ADR-0021 | Classified |
| LIMIT-0017 | Per-client event buffer (multi-client hub) | 1024 | `crates/synth_engine/src/hub.rs:145` |  | Configurable safety budget | Protocol contract | A live bounded queue fed by an external client, so runtime dropping is permitted — but per client, counted, and reported to that client rather than only to the host. | Per-client drop counter in the client's own diagnostics | ADR-0021 §1; ADR-0029 | ADR-0029 | Classified |
| LIMIT-0018 | OSC events per bundle | 32 | `crates/synth_osc/src/sender.rs:208` |  | Configurable safety budget | Protocol contract | A packing bound, not a loss: events beyond it travel in the next bundle. It belongs to the surface that serializes them. | N/A — nothing is lost; bundle count is observable | ADR-0021 §1; site read | ADR-0027 | Classified |
| LIMIT-0019 | OSC FFT band count | 256 | `crates/synth_osc_protocol/src/lib.rs:53` |  | Configurable safety budget | Protocol contract | A resolution budget of the telemetry protocol. Lowering it lowers resolution and never drops a message. | The band count travels with the payload, so a consumer cannot mistake resolution for content | ADR-0021 §1; ADR-0027 | ADR-0027 | Classified |
| LIMIT-0020 | Meter slots published to the GUI | 128 | `crates/synth_engine/src/state.rs:192` | `publish()` is `if let Some(slot) = self.slots.get(index)` — an index beyond the cap is silently dropped, and `set_count` clamps (`state.rs:234-245`). A project with more than 128 metered channels loses meters with no signal to the user. | Configurable safety budget | `HostProfile` | Observation taps become a `HostProfile` field. A plan requesting more taps than the profile allows is a **compile error**; silently dropping a meter is withdrawn. | Compile error naming the requested and available tap counts | ADR-0021 §3 | ADR-0021, ADR-0027 | Classified |
| LIMIT-0021 | Master scope visualization buffer | 4096 samples | `crates/synth_engine/src/state.rs:606` and `:677` (`VisualizationBuffer::new(4096)`) — passes 1-3 cited `:597`, which is `glide_time: AtomicF32::new(0.0)` and unrelated; there are also two construction sites, not one | **It drops the newest, not the oldest, and it is silent.** `master_scope` is written by `VisualizationBuffer::write_interleaved` (`visualizers/mod.rs:126-146`, called from `synth_engine.rs:4302`), which `push_slice`s each deinterleaved chunk and ignores the appended count; `write_samples` (`:112-121`) does the same and says so in its own comment, "dropping any that don't fit". So a full ring omits *new* samples rather than overwriting old ones, and `read_samples_into` (`:227-262`) exposes no omitted count or continuation marker. Both writers also skip the write entirely when the `try_lock` fails. **Ninth entry in the silent-truncation register**, and the reason the row's V2 rule below is a change rather than a restatement. **Partly fixed in V1 ahead of V2:** the writers now count what they could not fit, `read_samples_into` returns that count so the gap is paired with the window it belongs to, and `#[must_use]` stops a reader discarding it silently. **Open follow-up:** no GUI surface draws the gap yet — the three readers acknowledge the count and defer the rendering, because how to show a break in a waveform is a design question, not a bug fix. | Lossy retention/presentation budget | `HostProfile` | A telemetry ring holding non-authoritative presentation data. **V2 changes the behaviour**: HOST-INV-019 requires eviction by design *and* an exposed omitted count or continuation marker, and V1 has neither — it silently drops the newest samples. Its size is a profile budget because it costs prepared memory. | The reader can tell a complete window from a trimmed one — a requirement V1 does not meet today | ADR-0021 §1 | ADR-0027 | Classified |
| LIMIT-0022 | Spectrum-analyzer FFT size | 2048 | `crates/synth_engine/src/visualizers/spectrum_analyzer.rs:14` |  | Configurable safety budget | `HostProfile` | An analyzer resolution budget: it costs prepared memory and CPU, and changing it changes resolution rather than correctness. | The FFT size travels with the spectrum payload | ADR-0021 §1; ADR-0027 | ADR-0027 | Classified |

### Voices, modulation, and mixing

| ID | Resource/limit | Current value | Enforcement site | Overflow behavior | Limit class | Configuration owner | Proposed V2 rule | Diagnostic | Evidence | ADR | Status |
|------------|---------------------------------------------------------|--------------------------:|-------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------|---|------------------|----------------------------------------|----------|----------|---------------|
| LIMIT-0023 | Mod Matrix slots per voice | 16 | `crates/synth_core/src/params/mod_matrix.rs:13` | **Not coupled 1:1, corrected by the pass-4 use-site audit.** The compile-time assertion at `synth_modules/src/mod_matrix.rs:30` is `assert!(MAX_MOD_MATRIX_SLOTS <= synth_core::script::SCRIPT_HOST_SLOTS)` — an *inequality*. Lowering `SCRIPT_HOST_SLOTS` below the matrix slot count breaks the build, but raising it alone is legal today, so the two are a floor relation rather than one capacity. Passes 1-3 read the assertion as equality. | Configurable safety budget | `HostProfile` | Two profile fields, `mod_matrix_slots_per_voice` and `script_host_slots_per_voice`, with V1's `<=` relation validated in the profile constructor instead of by an assertion in a third crate. An earlier disposition collapsed them into one field on the mistaken 1:1 reading. | Compile error naming the patch and its slot count; the assertion that guards the coupling is no longer needed | ADR-0021 §1, §4; ADR-0007 | ADR-0007 | Classified |
| LIMIT-0024 | Channel sends per mixer channel (`MAX_CHANNEL_SENDS`) | 16 | `crates/synth_engine/src/synth_engine.rs:3683` — passes 1-3 cited `:3663`, which is `return TrackControl::NEUTRAL;` and unrelated | **Silent truncation, found by the pass-4 use-site audit.** `synth_engine.rs:3294` breaks out of the send loop at `list.len() >= MAX_CHANNEL_SENDS`. The break is tested *before* the disabled-send and missing-return-bus filters while `push` happens after them, so the bound is sixteen **successfully resolved** sends, not the first sixteen authored: a channel with twenty sends of which four are disabled renders all sixteen that resolve, and it is **not** "the first sixteen authored" — a disabled or unroutable send earlier in the list does not consume a slot. What is dropped, silently, is every resolved send after the sixteenth. The comment at `:3289` says "are dropped"; the ledger recorded nothing here for three passes. **Seventh entry in the silent-truncation register.** | Configurable safety budget | `HostProfile` | Sends are a named `HostProfile` field. A mixer topology exceeding it is refused at preparation, which is a **change** from V1's silent drop rather than a restatement of it; ADR-0034 owns what a send is. | Preparation error naming the channel and its send count | Use-site audit at `3555c52c`; ADR-0021 §2, §4; ADR-0034 | ADR-0034 | Classified |
| LIMIT-0025 | Mod Grid nodes per graph | 32 | `crates/synth_sequencer/src/mod_grid.rs:58` |  | Configurable safety budget | `HostProfile` | The `LIMIT-0067` precedent: Project Format V2 imposes no node cap and the document loads intact; compilation refuses a graph above the profile's node budget. | Compile error naming the graph and its node count | ADR-0021 §3 |  | Classified |
| LIMIT-0026 | Note Grid nodes per graph | 32 | `crates/synth_sequencer/src/note_graph.rs:1064` |  | Configurable safety budget | `HostProfile` | As `LIMIT-0025`. This is the constant `LIMIT-0067` truncates against, so the two dispositions must agree. | Compile error naming the graph and its node count | ADR-0021 §3 |  | Classified |
| LIMIT-0027 | Note-graph value inputs per node | 4 | `crates/synth_sequencer/src/note_graph.rs:174` |  | Platform capability | Node contract | Intrinsic to the node's shape rather than to any budget: a node declares how many value inputs it has, and the declaration reaches the `ResourceReport`. | Compile error naming the node and the input it cannot accept | ADR-0021 §1 |  | Classified |
| LIMIT-0028 | Unison ceilings — two separate concepts, not a conflict | 16 and 7 | `crates/synth_modules/src/voice_common.rs:9`, `crates/synth_modules/src/oscillator.rs:35` | **Resolved in pass 2: these are different features.** `MAX_UNISON = 16` bounds the shared choir sub-voice bank used only by `voice_synth.rs` and `fof.rs` (fixed arrays `[SubVoice; MAX_UNISON]`, no heap in `process()`); `MAX_UNISON_VOICES = 7` bounds the classic oscillator's own unison spread (fixed arrays in `oscillator.rs:81-87`). Neither module reads the other's constant. A third, unrelated unison concept exists at instrument level (`unison_detune`/`unison_spread` in `AllocatorConfig`). Both are array lengths, so exceeding them is impossible by construction — the parameter is mapped into range by `voice_common::unison_count`. | Platform capability | Node contract | Two unrelated node capacities, each an array length inside its own module. Each node declares its own; neither is a profile field the host may raise. | N/A — enforced by array length, and the parameter is mapped into range before it is read | ADR-0021 §1; pass-2 site read | ADR-0021 | Classified |
| LIMIT-0029 | MSEG segments | 16 | `crates/synth_modules/src/mseg.rs:25` |  | Platform capability | Node contract | A node-intrinsic array length, declared by the node into the `ResourceReport`. | Compile error naming the module and the segment count | ADR-0021 §1 |  | Classified |
| LIMIT-0030 | Voice steal fade | 128 samples | `crates/synth_engine/src/voice.rs:556` |  | Configurable safety budget | `HostProfile` | A retirement crossfade length, which is the `HostProfile` crossfade/retirement budget. ADR-0009 owns the value. | The retirement budget appears in the `ResourceReport` | ADR-0021 §4; ADR-0009 | ADR-0009 | Classified |
| LIMIT-0031 | Held-note tracking per allocator | 32 (`Vec::with_capacity`) | `crates/synth_engine/src/voice_allocator.rs:178` | Pre-allocated capacity, not a hard cap; exceeding it would allocate on the audio thread. | Implementation artifact to remove | `N/A — removed` | A `Vec::with_capacity` hint, not a bound: exceeding it allocates on the audio thread. V2 preallocates a declared held-note capacity, so the soft version ceases to exist. | N/A — the entry closes with the change; the replacement is admitted like any other profile capacity | ADR-0021 §1; site read | ADR-0021 | Classified |

### YAMS / script budgets

| ID | Resource/limit | Current value | Enforcement site | Overflow behavior | Limit class | Configuration owner | Proposed V2 rule | Diagnostic | Evidence | ADR | Status |
|------------|-------------------------------------|------------------------:|--------------------------------------------------|-------------------|----------------------------|---|------------------|------------|----------|----------|------------|
| LIMIT-0032 | Script instructions per program | 256 | `crates/synth_core/src/script/bytecode.rs:14` |  | Configurable safety budget | `HostProfile` | Script work multiplied by scope and polyphony is a named `HostProfile` field. A program above the budget is refused at compile. | Compile error naming the script and its instruction count | ADR-0021 §4; ADR-0008 | ADR-0008 | Classified |
| LIMIT-0033 | Script sources | 32 | `crates/synth_core/src/script/bytecode.rs:16` |  | Configurable safety budget | `HostProfile` | As `LIMIT-0032`: an execution capacity paid per voice. | Compile error naming the script and its source count | ADR-0021 §4; ADR-0008 | ADR-0008 | Classified |
| LIMIT-0034 | Script persistent state slots | 16 | `crates/synth_core/src/script/bytecode.rs:18` |  | Configurable safety budget | `HostProfile` | As `LIMIT-0032`. State slots are prepared memory multiplied by polyphony, which is why the profile owns them rather than the language. | Compile error naming the script and its state usage | ADR-0021 §4; ADR-0008 | ADR-0008 | Classified |
| LIMIT-0035 | Script locals | 16 | `crates/synth_core/src/script/bytecode.rs:20` |  | Configurable safety budget | `HostProfile` | As `LIMIT-0032`. | Compile error naming the script and its local count | ADR-0021 §4; ADR-0008 | ADR-0008 | Classified |
| LIMIT-0036 | Script evaluation stack | 64 | `crates/synth_core/src/script/bytecode.rs:22` |  | Configurable safety budget | `HostProfile` | As `LIMIT-0032`: stack depth is a bound on prepared per-voice memory. | Compile error naming the script and the expression that overflowed | ADR-0021 §4; ADR-0008 | ADR-0008 | Classified |
| LIMIT-0037 | Script nesting depth | 32 | `crates/synth_core/src/script/bytecode.rs:24` |  | Configurable safety budget | Domain/format contract | A parser bound on the *representation* of a script rather than on its execution: a file carries the nesting, so changing it is a format question. | Parse error naming the position and the depth | ADR-0021 §1; ADR-0008 | ADR-0008 | Classified |
| LIMIT-0038 | Script source length | 4096 chars | `crates/synth_core/src/script/bytecode.rs:26` |  | Configurable safety budget | Domain/format contract | As `LIMIT-0037`: a bound on what a saved script may contain, enforced where the document is validated. | Validation error naming the script and its length | ADR-0021 §1; ADR-0008 | ADR-0008 | Classified |
| LIMIT-0039 | Script arrays / total array storage | 16 arrays, 256 elements | `crates/synth_core/src/script/bytecode.rs:28-30` |  | Configurable safety budget | `HostProfile` | As `LIMIT-0032`: array storage is prepared memory multiplied by polyphony. | Compile error naming the script and its array storage | ADR-0021 §4; ADR-0008 | ADR-0008 | Classified |
| LIMIT-0040 | Script exposed parameters | 32 | `crates/synth_core/src/script/bytecode.rs:36` |  | Configurable safety budget | Domain/format contract | The exposed-parameter list is a patch interface a file carries, so the format owns its width. | Validation error naming the script and its parameter count | ADR-0021 §1; ADR-0008 | ADR-0008 | Classified |
| LIMIT-0041 | Script host slots | 16 | `crates/synth_core/src/script/host.rs:22` |  | Configurable safety budget | `HostProfile` | **Related to `LIMIT-0023` by a `<=` floor, not an equality** — but V1 does not exercise the difference: at `3555c52c` the only production `ScriptHost::new()` is the Mod Matrix's own (`synth_modules/src/mod_matrix.rs:55`), the rest being tests. Pass 4 claimed module-agnosticism implies independent consumers; the type is generic, and that is all it shows. The profile carries its own field floored at the matrix slot count as a **V2 design choice**, which Phase 7 may reverse. | Compile error naming the patch and its slot count | ADR-0021 §4; ADR-0007 | ADR-0008 | Classified |
| LIMIT-0042 | Script output slots | 4 | `crates/synth_core/src/script/eval.rs:155` |  | Configurable safety budget | `HostProfile` | Script emits are a named `HostProfile` field, paid per voice per block. | Compile error naming the script and its output count | ADR-0021 §4; ADR-0008 | ADR-0008 | Classified |

### Sequencer and note processing

| ID | Resource/limit | Current value | Enforcement site | Overflow behavior | Limit class | Configuration owner | Proposed V2 rule | Diagnostic | Evidence | ADR | Status |
|------------|------------------------------------------------|-----------------------:|------------------------------------------------------------|--------------------------------------------------------------------------------|----------------------------|---|------------------|------------|----------|-----|---------------|
| LIMIT-0043 | Note-processor expansion events per tick | 128 | `crates/synth_sequencer/src/note_processor.rs:45` | **Counted, not silent — and pass 4 got this wrong before review corrected it.** Overflow is handled at `ExpansionBuffer::push` (`note_processor.rs:1518-1527`): the note is rejected and `dropped` incremented, never reallocated. `sequencer_engine.rs:755` and `:880` accumulate it into `expansion_drops`. Pass 4 read the `.min()` at `:1318` and called it a silent truncation; it is a defensive no-op, since `push` guarantees `notes()` never exceeds capacity. **The real gap is the `LIMIT-0013` pattern**: `expansion_drops()` (`sequencer_engine.rs:1105`) has no caller outside its own module, so the count exists and reaches nobody. Backs the engine's `scratch_notes` pre-allocation (`sequencer_engine.rs:339`). | Configurable safety budget | `HostProfile` | Note expansion is event fan-out, which is a `HostProfile` field. Part 2 forbids trimming an expansion to fit, so an over-expanding tick is a refusal. | Preparation error naming the pattern and the tick that exceeds the budget | ADR-0021 §2, §4 |  | Classified |
| LIMIT-0044 | Ornament hits | 64 | `crates/synth_sequencer/src/note_processor.rs:52` |  | Platform capability | Node contract | An array length inside the ornament processor, declared by the node. | Compile error naming the processor and its hit count | ADR-0021 §1 |  | Classified |
| LIMIT-0045 | Note delay ticks / repeats | 7680 ticks, 16 repeats | `crates/synth_sequencer/src/note_processor.rs:57,73` |  | Configurable safety budget | Domain/format contract | Authored parameter ranges — a note delay of at most 7 680 ticks is part of what a note may say — so the document format validates them. | Validation error naming the note and the value | ADR-0021 §1 |  | Classified |
| LIMIT-0046 | Note-processor lookback depth | 2 | `crates/synth_sequencer/src/note_processor.rs:68` |  | Platform capability | Node contract | A node-intrinsic lookback window, declared by the node. | Compile error naming the processor | ADR-0021 §1 |  | Classified |
| LIMIT-0047 | Ratchet subdivisions | 16 | `crates/synth_sequencer/src/note_processor.rs:78` |  | Platform capability | Node contract | A node-intrinsic array length. | Compile error naming the processor and the subdivision count | ADR-0021 §1 |  | Classified |
| LIMIT-0048 | Envelope-stream window | 960 ticks | `crates/synth_sequencer/src/note_processor.rs:86` |  | Platform capability | Node contract | A node-intrinsic window length. | Compile error naming the processor | ADR-0021 §1 |  | Classified |
| LIMIT-0049 | Chord intervals / arp offsets / arp held notes | 8 / 16 / 16 | `crates/synth_sequencer/src/note_processor.rs:308,532,619` |  | Platform capability | Node contract | Three node-intrinsic array lengths in the chord and arpeggio processors. | Compile error naming the processor and the array that is full | ADR-0021 §1 |  | Classified |
| LIMIT-0050 | Step-LFO steps / script note inputs | 64 / 64 | `crates/synth_sequencer/src/note_graph.rs:424,688` |  | Platform capability | Node contract | Two node-intrinsic array lengths in the note-graph nodes. | Compile error naming the node and the array that is full | ADR-0021 §1 |  | Classified |

### Recording, session, and application stores

| ID | Resource/limit | Current value | Enforcement site | Overflow behavior | Limit class | Configuration owner | Proposed V2 rule | Diagnostic | Evidence | ADR | Status |
|------------|-------------------------------------------|----------------------------------------------------------------:|-----------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------|----------------------------|---|------------------|------------|----------|----------|---------------|
| LIMIT-0051 | Recording buffers (held / recorded notes) | 32 / 4096 | `crates/synth_engine/src/recording.rs:9,12` | Capacity is restored after flush (`recording.rs:387-393`) so the audio thread never reallocates. | Configurable safety budget | `HostProfile` | Recording capacities are a named `HostProfile` field. Capacity is preallocated and restored after flush, so the audio thread never reallocates; a take that would exceed it stops with a counted diagnostic rather than dropping notes. | Recording diagnostic naming the take and the capacity reached | ADR-0021 §4; ADR-0024 | ADR-0024 | Classified |
| LIMIT-0052 | Activity log ring | 1000 entries | `crates/pertylizer/src/activity_log.rs:26` |  | Lossy retention/presentation budget | Application settings | A presentation ring of non-authoritative history: the oldest entries are evicted by design. | The evicted count is exposed with the log | ADR-0021 §1 | ADR-0027 | Classified |
| LIMIT-0053 | Recovery snapshots retained | 20 | `crates/pertylizer/src/recovery.rs:47` |  | Lossy retention/presentation budget | Application settings | Recovery generations are explicitly evictable and are not project truth. | The retained generation count and the evicted count are exposed in the recovery surface | ADR-0021 §1 |  | Classified |
| LIMIT-0054 | Recent projects retained | 10 | `crates/pertylizer/src/io/settings.rs:144` |  | Lossy retention/presentation budget | Application settings | A recent-item list, the class's own example. | N/A — the list is visibly complete or visibly trimmed | ADR-0021 §1 |  | Classified |
| LIMIT-0055 | Engine snapshot sync timeouts | 2000 ms (`project_apply.rs:1186`), 5000 ms (`mcp_bridge.rs:74`) | `crates/pertylizer/src/project_apply.rs:1186`, `crates/pertylizer/src/mcp_bridge.rs:84` | Two different timeouts guard the same "wait for the engine to publish a snapshot" problem on the save path. | Implementation artifact to remove | `N/A — removed` | Two timeouts guarding one race that V2 does not have: Project Core V2 is the save authority, so a save reads the document rather than waiting for the engine to publish a snapshot. | N/A — the entry closes with the change | ADR-0021 §1; ADR-0035 | ADR-0035 | Classified |

### Host I/O and identity capacities (pass 2)

| ID | Resource/limit | Current value | Enforcement site | Overflow behavior | Limit class | Configuration owner | Proposed V2 rule | Diagnostic | Evidence | ADR | Status |
|------------|--------------------------------------|-------------------------------------------------------------:|------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------|---|------------------|-------------------------|----------|--------------------|---------------|
| LIMIT-0056 | Voices per instrument (`VoiceCount`) | clamped to [1, 128], default 8 (`OCTO`) | `crates/synth_core/src/types/audio.rs:470` | `count.clamp(1, 128)` — a request outside the range is silently changed to the nearest bound. `new_unchecked` only `debug_assert!`s, so a release build accepts an out-of-range value. This is the global polyphony ceiling pass 1 recorded as missing. | Configurable safety budget | `HostProfile` | Polyphony is authored data, so part 2 forbids the clamp. A voice count outside the profile range is a **preparation error**, `VoiceCount` construction becomes fallible, and the release-build `new_unchecked` hole is removed. | Preparation error naming the instrument, the requested count, and the permitted range | ADR-0021 §2, §3 | ADR-0021 | Classified |
| LIMIT-0057 | Advertised device buffer-size range | `BufferSize::SMALL` (128) .. `BufferSize::VERY_LARGE` (1024) | `crates/pertylizer/src/audio/backends/cpal_backend.rs:74,97,105` | **Hardcoded on all three branches**, including the branch that successfully queried `supported_output_configs()` — the device's own reported range is discarded. The stream is then requested as `cpal::BufferSize::Fixed` (`cpal_backend.rs:275`), but the callback's real frame count is recomputed from `data.len()` (`cpal_backend.rs:321`), so a host that ignores the request is what makes `LIMIT-0001` reachable at all. | Implementation artifact to remove | `N/A — removed` | Capability fields are built from queried device capability, so a hardcoded advertised range does not survive. This entry is the anti-pattern ADR-0021 §4 names. | N/A — the entry closes with the change; the queried range appears in the `ResourceReport` | ADR-0021 §4 | ADR-0021, ADR-0022 | Classified |
| LIMIT-0058 | Module instances per type per graph | 65 535 (`ModuleId.instance: u16`) | `crates/synth_engine/src/commands.rs:30-33`, allocated in `graph.rs:177-181` | `*counter += 1` on a `u16` with no ceiling check: overflow panics in debug and wraps in release. Instance numbers are never reused after a module is removed, so a long editing session monotonically climbs. Reaching it is implausible, but nothing states that. | Platform capability | Domain/format contract | An identity-encoding ceiling, so ADR-0014 owns generation and width. Exhaustion becomes an explicit refusal; a `u16` that panics in debug and wraps in release is neither a limit nor a diagnostic. | Allocation error naming the graph and the exhausted id space | ADR-0021 §1; ADR-0014 | ADR-0014, ADR-0021 | Classified |
| LIMIT-0059 | Audio channel layouts | `Mono`, `Stereo`, `Multi(u16)` | `crates/synth_core/src/audio/types.rs:131-136` | `Multi(u16)` exists in the type but no production path constructs it; every buffer in the engine is mono or interleaved stereo. The layout ceiling pass 1 looked for is therefore "2 by construction", not by an enforced limit. | Implementation artifact to remove | `N/A — removed` | An enum variant nothing constructs. ADR-0002 decides the internal channel layout, and the unused representation goes with the V1 engine. | N/A — the entry closes with the change | ADR-0021 §1; ADR-0002 | ADR-0002 | Classified |
| LIMIT-0060 | Graph edges and fan-out | **No limit** | `crates/synth_engine/src/graph.rs` | Connections are a plain `Vec<Connection>` with no cap, and fan-out is unbounded. Pass 1 flagged this area as "absence must be confirmed, not assumed"; pass 2 confirms the absence. V2 must decide whether unbounded is intended or merely unimplemented. | Configurable safety budget | `HostProfile` | Unbounded is unimplemented rather than intended: nodes, edges, and fan-out become profile budgets reported in the `ResourceReport`, so a plan that cannot be admitted says so at compile. | Compile error naming the graph and the budget it exceeds | ADR-0021 §1, §4; ADR-0033 | ADR-0021, ADR-0033 | Classified |
| LIMIT-0061 | Sample zones per sampler | **No zone model exists** | — | `N/A — the V1 sampler holds exactly one sample per module, referenced by `sample_select.sample_id`. There is no `SampleMap`/`SampleZone` type to bound.` The required-area entry is satisfied by absence, and ADR-0026 designs the model rather than migrating one. | Configurable safety budget | Domain/format contract | There is no V1 limit because there is no model. ADR-0026 introduces `SampleMap`/`SampleZone`, and the zone count becomes a domain bound reported into admission. | Validation error naming the map and its zone count, once the model exists | ADR-0021 §1; ADR-0026 | ADR-0026 | Classified |
| LIMIT-0062 | Telemetry taps | **Fixed set, not registered** | `crates/synth_engine/src/visualizers/` | `N/A as a count — there are three fixed visualizer kinds (level meter, oscilloscope, spectrum analyzer) writing into a shared `VisualizationBuffer`; nothing registers a tap dynamically.` The bounded resources are `LIMIT-0020`, `LIMIT-0021`, and `LIMIT-0022`. | Configurable safety budget | `HostProfile` | V1's three fixed visualizer kinds are replaced by registered taps bounded by the profile, which is what `LIMIT-0020`'s disposition already requires. | Compile error naming the requested and available tap counts | ADR-0021 §3, §4; ADR-0027 | ADR-0027 | Classified |

### Application-session budgets (pass 2)

| ID | Resource/limit | Current value | Enforcement site | Overflow behavior | Limit class | Configuration owner | Proposed V2 rule | Diagnostic | Evidence | ADR | Status |
|------------|--------------------------------------------|----------------------------------:|--------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------|---|------------------|---------------------------------------------|----------|----------|---------------|
| LIMIT-0063 | Undo history depth | 100 actions | `crates/pertylizer/src/undo.rs:18` | Oldest entries are discarded. Discarding an entry makes `is_dirty`'s "back at the saved stack depth" shortcut unreachable for work older than the window. | Lossy retention/presentation budget | Application settings | Undo depth is retention of non-authoritative history, evicted by design. ADR-0015 owns the representation. | The evicted count is exposed, and the dirty term stops relying on stack depth for work older than the window | ADR-0021 §1; ADR-0015 | ADR-0015 | Classified |
| LIMIT-0064 | Sample audio retained by undo history | 256 MiB | `crates/pertylizer/src/undo.rs:31` | A bound on *retained* audio, not on a single edit: one oversized sample stays undoable exactly once. Trimming silently reduces how far back a destructive sample edit can be undone. | Lossy retention/presentation budget | Application settings | Retained undo audio, the class's central case: eviction is explicit and the loss is how far back a destructive edit can be undone. | The retained-audio figure and what eviction has already cost are visible in the history surface | ADR-0021 §1; ADR-0015 | ADR-0015 | Classified |
| LIMIT-0065 | Parameter-gesture merge window | 600 ms | `crates/pertylizer/src/undo.rs:39` | Two edits to the same control inside the window merge into one undo step. Not a truncation — a granularity choice. | Configurable safety budget | Application settings | A granularity setting rather than a bound: exceeding the window creates a second undo step and loses nothing. | N/A — no loss to report | ADR-0021 §1; ADR-0015 | ADR-0015 | Classified |
| LIMIT-0066 | Autosave debounce interval | 30 s | `crates/pertylizer/src/gui/egui_backend/autosave_flow.rs:18` | At most one snapshot in flight; a second would race the first for the same file. A failed write updates the attempt time, so a broken recovery directory retries on the interval rather than every frame. | Configurable safety budget | Application settings | An editor cadence, unrelated to any render plan. Its existing once-per-session status report already meets the class's requirement. | The existing status-bar report of a failing autosave | ADR-0021 §1; ADR-0024 | ADR-0024 | Classified |
| LIMIT-0067 | Note-processor rack → Note Grid conversion | `MAX_NOTE_GRID_NODES` = 32 stages | `crates/synth_sequencer/src/song.rs:881-887` | A rack longer than the node cap stops at the cap, **silently dropping the excess stages** on load. The comment states a hard error was rejected because it would block the load. | Configurable safety budget | `HostProfile` | V1's false choice disappears once document and plan are separate: **Project Format V2 imposes no stage cap and the document loads with every stage intact**, while compilation refuses a rack above the node budget. Silent dropping is withdrawn. | Compile error naming the rack and its stage count; the load itself no longer refuses or truncates | ADR-0021 §3 | ADR-0021 | Classified |

### MCP reply truncation (pass 2 — identifiers allocated)

Pass 1 left these unnumbered pending a granularity decision. Pass 2 allocates one identifier each and records the open
question rather than the answer: these truncate a *reply*, so they likely belong to a protocol contract rather than to
`RenderLimits`, and ADR-0027 owns that split.

| ID | Resource/limit | Current value | Enforcement site | Overflow behavior | Limit class | Configuration owner | Proposed V2 rule | Diagnostic | Evidence | ADR | Status |
|------------|-----------------------------------------------------|---------------:|-------------------------------------------------------|-------------------------------------------------------|----------------------------|---|------------------|------------|----------|----------|------------|
| LIMIT-0068 | Batch-report summary and log-detail truncation | 60 / 400 chars | `crates/synth_mcp/src/server.rs:5151,5156` | Truncated by character count | Lossy retention/presentation budget | Protocol contract | A presentation summary whose full detail remains available, which is exactly what the lossy class permits — provided the truncation is visible. | A continuation marker or a pointer to the untruncated detail travels with the reply | ADR-0021 §1; ADR-0027 | ADR-0027 | Classified |
| LIMIT-0069 | Module search default result limit / type-key hints | 20 / 3 | `crates/synth_mcp/src/server/tools/discovery.rs:6,12` | Default only — the search limit is caller-overridable | Configurable safety budget | Protocol contract | A caller-overridable default, not a cap. The protocol owns the default and the maximum a caller may ask for. | The applied limit travels with the reply, so a short result is distinguishable from a complete one | ADR-0021 §1; ADR-0027 | ADR-0027 | Classified |
| LIMIT-0070 | Module description length accepted over MCP | 2000 chars | `crates/pertylizer/src/mcp_bridge.rs:84` | Validation boundary, not a truncation | Configurable safety budget | Protocol contract | A validation boundary: an over-long description is **rejected**, never stored truncated. | Validation error naming the field and the permitted length | ADR-0021 §1, §2; ADR-0027 | ADR-0027 | Classified |
| LIMIT-0071 | Diagnostics included in a summary reply | 10 | `crates/pertylizer/src/project_diagnostics.rs:399` | Excess diagnostics omitted from the summary | Lossy retention/presentation budget | Protocol contract | An omission from a summary whose full diagnostic set is retrievable. | The omitted count travels with the summary | ADR-0021 §1; ADR-0027 | ADR-0027 | Classified |

### Per-module DSP capacities (pass 2 — grouped by admission class)

Pass 1 left roughly 30 module-local ceilings unnumbered pending a granularity decision. Pass 2 groups them by the *kind*
of resource they bound, because that is the shape a V2 admission rule would take — one declaration per resource class
per node, not one rule per module. Every member is named so nothing is hidden behind the grouping.

| ID | Resource/limit | Current value | Enforcement site | Overflow behavior | Limit class | Configuration owner | Proposed V2 rule | Diagnostic | Evidence | ADR | Status |
|------------|--------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------:|----------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------|---------------------|---|------------------|------------|----------|----------|---------------|
| LIMIT-0072 | Grain pools | 96 / 64 / 32 grains | `synth_modules/src/fof.rs:64`, `effects/granular_fx.rs:16`, `granular_osc.rs:28` | Fixed-size pools; a request beyond the pool steals or skips a grain | Platform capability | Node contract | Each node declares its grain pool into the `ResourceReport`. Grain stealing stays, because it is a documented DSP behavior rather than an admission failure — but the node exposes how often it stole. | Stolen-grain count per node in the structured diagnostics report | ADR-0021 §1 | ADR-0021 | Classified |
| LIMIT-0073 | Prepared audio memory per node | source 96 000 samples (`granular_osc.rs:34`), IR 96 000 samples (`effects/convolver.rs:25`), delay lines 2.0 s (`effects/delay.rs:18`) / 4.0 s (`effects/granular_fx.rs:17`) / 25 ms (`effects/ensemble_chorus.rs:17`) / 20 ms (`effects/flanger.rs:13`) / 4800 samples (`math_oscillator.rs:28`), FFT 4096 (`effects/phase_vocoder.rs:20`, `effects/spectral_blur.rs:18`) and 2048 (`effects/vocoder.rs:19`), oversampling intermediate 8192 (`oversampling.rs:139`), pitch-tracker ring 2048 (`pitch_tracker.rs:20`), sweep 19 200 (`signal_monitor.rs:30`), SID decimation 4096 (`sid_oscillator.rs:45`), wavetable/padsynth tables 2048/4096 (`wavetable_data.rs:15`, `padsynth.rs:26`) | as listed | Truncation or clamping, per module; `phase_vocoder.rs:252` and `spectral_blur.rs:202` clamp explicitly, the rest not verified | Platform capability | Node contract | Each node declares its prepared bytes; **the aggregate is admitted against the profile's prepared-memory budget**, which is the finding this row carries — V1 computes no aggregate anywhere. | Compile error naming the dominant contributors when the aggregate exceeds the budget; the `ResourceReport` lists them either way | ADR-0021 §1, §4 | ADR-0021 | Classified |
| LIMIT-0074 | Model-order ceilings | vocal tract 54 sections (`vocal_tract.rs:37`), LPC order 32 (`math.rs:764`), padsynth harmonics 64 (`padsynth.rs:32`), modal resonator 16 modes (`effects/modal_resonator.rs:18`), euclidean 32 steps (`euclidean.rs:16`), early reflections 6 taps (`spatial_panner/early_reflections.rs:24`), ensemble chorus 3 voices (`effects/ensemble_chorus.rs:19`) | as listed | Parameter ranges are mapped into the ceiling; not verified per module | Platform capability | Node contract | Model orders are node-intrinsic and declared. Parameters are mapped into range before they are read, so the ceiling is not reachable from a patch. | N/A — parameter mapping keeps requests in range; the ceiling appears in the node's declaration | ADR-0021 §1 | ADR-0021 | Classified |

## Required areas

Include render quantum, layouts, voices, nodes, graph edges, events and fan-out, channels, buses, sends, buffers,
telemetry taps, recording buffers, command and event queues, prepared memory, sample zones, and YAMS/script work.

Every area on pass 1's not-reached list is now answered:

| Area                            | Status after pass 2                                                                                                |
|---------------------------------|--------------------------------------------------------------------------------------------------------------------|
| Channel layouts                 | `LIMIT-0059` — mono/stereo by construction; `Multi(u16)` exists in the type but nothing constructs it              |
| Graph edges and event fan-out   | `LIMIT-0060` — absence confirmed, not assumed: unbounded `Vec`, no cap                                             |
| Voice-count ceiling             | `LIMIT-0056` — `VoiceCount` silently clamps to [1, 128]                                                            |
| Sample zones                    | `LIMIT-0061` — no zone model exists in V1 to bound                                                                 |
| Telemetry taps                  | `LIMIT-0062` — a fixed set of three visualizer kinds, not a registration surface                                   |
| Prepared memory as an aggregate | `LIMIT-0073` — grouped by resource class; **no aggregate is computed anywhere today**, which is itself the finding |

### Silent-truncation register

The Phase 0A exit gate requires that no unexplained silent truncation is accepted as baseline behavior. Pass 2's second
discovery method found these, each confirmed by reading the site:

| Entry        | What is silently lost                                                                                  |
|--------------|--------------------------------------------------------------------------------------------------------|
| `LIMIT-0001` | The tail of an oversized audio-input block; and, on the output path, real-time safety rather than data |
| `LIMIT-0013` | Low-priority events under load — **counted**, so this one is explained, not silent                     |
| `LIMIT-0020` | Meters beyond channel 128                                                                              |
| `LIMIT-0056` | An out-of-range voice count, changed to the nearest bound                                              |
| `LIMIT-0067` | Note-processor rack stages beyond 32, dropped at load time                                             |
| `LIMIT-0004` | **Found by pass 3, not by either search — and since fixed.** Half the advertised limiter look-ahead above 192 kHz |
| `LIMIT-0024` | **Found by the pass-4 use-site audit.** Resolved channel sends after the sixteenth, dropped per block at `synth_engine.rs:3294` |
| `LIMIT-0014` | **Found by external review during pass 4.** `EngineEvent` pushes discard `try_push` failures (`synth_engine.rs:2165,2206,2278`); the `NoteEvent` side counts |
| `LIMIT-0021` | **Found by external review during pass 4.** The visualization ring drops the *newest* samples and exposes no omitted count. `master_scope`'s actual writer is `write_interleaved` (`visualizers/mod.rs:126-146`, called from `synth_engine.rs:4302`); `write_samples` (`:112-121`) does the same |
| `LIMIT-0043` | **Reclassified during pass 4.** Expansion overflow *is* counted, but `expansion_drops()` (`sequencer_engine.rs:1105`) has no consumer, so the loss is silent to users |

**The register is now ten entries, and the last four were found by classifying or by reading use sites — never by
searching.** Only the original five carry ADR-0021 §3 dispositions. `LIMIT-0004`, `LIMIT-0024`, and `LIMIT-0021` fall
under their entries' class rules rather than under that record's site-by-site list, so none of the three triggers its
taxonomy revisit condition. **`LIMIT-0014` has no settled class rule at all** — its row is `Investigating` with an
undecided owner until the GUI and OSC rings are split — so it is neither disposed nor covered. Four consecutive
discoveries outside the register is itself the finding.

`LIMIT-0024` is the seventh site, and the third that no search found. `MAX_CHANNEL_SENDS` is 16
(`synth_engine.rs:3683`); the per-block send loop breaks at `list.len() >= MAX_CHANNEL_SENDS`, so a channel authored
with more than sixteen sends renders the first sixteen that **resolve** and discards every resolved send after that,
every block, with no counter and no diagnostic. The break is tested before the disabled-send and missing-return-bus
filters while `push` happens after them, so a disabled or unroutable send earlier in the list does not consume a slot —
the bound is on resolved sends, not on authored ones. The comment at `:3289` says so in prose — which is what makes it a pass-2 miss as well: that pass searched
for `silently (cap|truncat|drop|ignor)`, and this comment says "are dropped". Its V2 disposition already refuses an
over-sent topology at preparation, so the taxonomy holds and ADR-0021's revisit condition is not triggered; what was
incomplete is the register, for the third time.

**`LIMIT-0043` belongs in the register too, for a reason the audit first got wrong and then understated.** It reported
the entry as a site by reading the `.min()` at `note_processor.rs:1318`; that is a defensive no-op, since
`ExpansionBuffer::push` (`:1518-1527`) rejects and counts on overflow. But the count reaches nobody —
`expansion_drops()` (`sequencer_engine.rs:1105`) has no caller outside its module — and that is exactly the criterion
under which `LIMIT-0013`'s disposition was withdrawn. **A drop nobody can observe is silent.** Tenth entry.

`LIMIT-0004` is the sixth site, and it was found by classifying rather than by searching. The render command accepts up
to 384 kHz (`render/mod.rs:34`) while `SampleRate::MAX_SUPPORTED` — documented as "the engine-wide ceiling", and the
value real-time look-ahead buffers size themselves from — is 192 kHz. `SampleRate::new` validates only positivity, so
nothing rejects the difference. The limiter's look-ahead ring is `0.005 x 192 000 = 960` frames and its request is
`clamp(1, MAX_LOOKAHEAD_SAMPLES)` (`effects/limiter.rs:26,68-70`), so a 384 kHz render silently delivers 2.5 ms of the
5 ms the parameter advertises. Nothing overflows and nothing is unsafe; the audio is simply not what the patch asked
for, with no diagnostic. Any other module that sizes from the ceiling has the same shape.

**This does not trigger ADR-0021's revisit condition.** That condition fires when a truncation is found that no class
rule covers, which would mean the taxonomy is incomplete. This one is covered: it is a `HostProfile`-owned range whose
rule is a refusal at job admission. What it shows is that the *register* was incomplete — the weakness both earlier
passes recorded about themselves — and it is a third argument for the executable probe ADR-0021 lists as follow-up.

**It is fixed in V1 rather than left for cutover.** `MAX_RENDER_SAMPLE_RATE` now derives from
`DeviceSampleRate::MAX_SUPPORTED` instead of restating it, so the two cannot desync again, and three tests pin the
result: the rate above the ceiling is refused, the derivation holds, and the render bounds' product still sits under
`MAX_RENDER_BYTES`. That last one is a consequence worth recording — with the ceiling halved, **no legal request can
reach the size guard any more**, so it becomes a backstop against the other three bounds moving rather than a check
that fires. The two measurement harnesses got the same ceiling, so a future baseline cannot silently be taken on
degraded DSP.

**Of the nine, only the original five have an accepted disposition, and one of those five has since been withdrawn.**
[ADR-0021 §3](../decisions/ADR-0021-host-profile-and-admission-policy.md#3-disposition-of-the-five-silent-truncation-sites)
covers `LIMIT-0001`, `LIMIT-0013`, `LIMIT-0020`, `LIMIT-0056`, and `LIMIT-0067` — four changing behaviour, and
`LIMIT-0013` keeping runtime dropping "with its counters promoted out of OSC-only publication". **That last one is
withdrawn**: the pass-4 use-site audit showed the counters are published nowhere and the queue is engine egress, so the
disposition rests on a reading that does not hold, and the row is `Needs review` pending an ADR-0021 successor.

The four later entries — `LIMIT-0004`, `LIMIT-0024`, `LIMIT-0014`, and `LIMIT-0021` — are **outside** that record's
site-by-site list. Three of them are covered by their own entry's class rule, so none triggers ADR-0021's taxonomy revisit condition, but
none has the site-specific disposition the original five got. **`LIMIT-0014` is not even that**: its row is
`Investigating` with an undecided owner, so it has no settled class rule to fall under until the GUI and OSC rings are
split. **The exit gate's "no unexplained silent
truncation" clause is therefore not satisfied by ADR-0021 alone.** What remains open is the register's *completeness* — and, for `LIMIT-0014`, the disposition itself, since that row is `Investigating` with an undecided owner and cannot be covered by a settled class rule until it is split. On completeness: a truncation that is
both unnamed and undocumented is invisible to all three passes, and closing that needs the executable probe ADR-0021
lists as follow-up work.

## Audit passes

| Date       | Source revision | Search/measurement method                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Coverage/result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Evidence                           |
|------------|-----------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------|
| 2026-08-12 | `dd69b657`      | `rg` over `crates/**/*.rs` for `const <NAME>: <int/float> =` where the name contains `MAX`, `CAP`, `CAPACITY`, `LIMIT`, `SIZE`, `COUNT`, `BUDGET`, `SLOTS`, `QUANTUM`, or `BLOCK`; plus targeted reads of `event_priority.rs`, `synth_engine.rs`, `recording.rs`, and `render/mod.rs` for enforcement and overflow behavior. Benches and test modules excluded.                                                                                                                                       | 55 entries allocated; two families deliberately left unnumbered. Constant-name pattern matching is the known weakness: a cap expressed as a literal at its use site, as a `Vec::with_capacity` argument, or as a type-level array length is invisible to it. No measurement was performed — every value here is read from source, not observed.                                                                                                                                                                                                                     | Pending `EVD` record for P00A-T004 |
| 2026-08-14 | `3555c52c`      | **Pass 4 — use-site audit of the 30 entries that were `HostProfile`-owned when it started.** Prompted by external review finding that `LIMIT-0014`'s constant is an egress ring rather than a renderer scratch. Method: resolve every cited `file:line`, then read *every* use of the named constant, rather than the definition alone. This is the first pass to check what a constant is used for. | **Five findings, plus two the pass got wrong — and it reopened three rows.** `LIMIT-0013`, `LIMIT-0014`, and `LIMIT-0015` leave `Classified`: the first because ADR-0021's disposition for it rests on two disproved readings, the second because one constant sizes two rings with different destinations and ADR-0021 requires one owner per entry, the third because its overflow behaviour is only partly read. **72 of 75 entries are `Classified`, not 75.** `LIMIT-0015` is not a return-bus scratch at all — it sizes four deferred-drop channels — so it moves from `HostProfile` to `N/A — removed`. With `LIMIT-0014` becoming undecided and `LIMIT-0075` added, the post-pass distribution is **28 `HostProfile` / 1 undecided / 46 elsewhere across 75 entries**; 29/45 was the intermediate state after the `LIMIT-0015` move alone. `LIMIT-0014` corrected: V1 has **no per-quantum event limit**. Two silent-truncation sites found that three passes missed, both in entries whose truncation column was empty — `LIMIT-0024` (sends beyond 16 dropped per block, `synth_engine.rs:3294`) and `LIMIT-0043` — but **the second of those was itself wrong**, and review caught it: expansion overflow is rejected and counted at `ExpansionBuffer::push`, so the register goes to seven, not eight. What `LIMIT-0043` actually has is the `LIMIT-0013` pattern, a counter with no reader outside its module. Two citations pointed at unrelated lines (`LIMIT-0021`, `LIMIT-0024`). `LIMIT-0023`/`LIMIT-0041` are a `<=` floor relation, not a 1:1 coupling. **Two findings were wrong, and both failed the way the audit was convened to correct.** Three constants reported as unregistered are `LIMIT-0037`, `LIMIT-0038`, and `LIMIT-0040` — missed because the check searched for constant *identifiers* while those rows name their limits in prose. And `LIMIT-0043`'s "silent truncation" was a defensive `.min()`, asserted without reading the `push` that feeds it. An audit whose subject was "passes 1-3 read definitions instead of uses" made that same class of error twice in its own findings; external review caught both. **What passes 1-3 shared is that all three read definitions**: pass 1 matched constant names, pass 2 matched authors' own truncation comments, pass 3 read enforcing code for entries it already believed in. None asked what a constant is used for. | This row |
| 2026-08-13 | `b435887c`      | **Pass 3 — classification, not discovery.** Applied accepted ADR-0021 to every existing entry: a terminal failure class, one of seven configuration owners, a proposed V2 rule, and a diagnostic, each citing the ADR clause that supports it. No new search was run, so no entry was added. | All 74 entries `Classified`; the 19 `Unknown` classes resolved; the `Configuration owner` column added and filled. **A sixth silent-truncation site fell out of classifying rather than searching** — `LIMIT-0004`, where a render above 192 kHz silently halves the limiter's advertised look-ahead — plus two smaller findings: `LIMIT-0073` (no prepared-memory aggregate is computed anywhere, so the profile's budget has nothing to admit against yet), and `LIMIT-0023`/`LIMIT-0041` (one capacity recorded as two constants held together by a compile-time assertion). Still no measurement: a class is a policy, not an observation. `LIMIT-0004` was fixed in V1 immediately afterwards. | This row |
| 2026-08-12 | `dd69b657`      | Second method, aimed at *behavior* rather than at constant names: (a) `rg -i 'silently (cap\|truncat\|drop\|ignor)'` over production code, which finds truncation sites documented by their own authors regardless of how the bound is expressed; (b) targeted reads of `synth_engine::process()`, `AudioBuffer::resize`, `cpal_backend`, `VoiceCount`, `ModuleId`, `graph.rs`, `undo.rs`, and `autosave_flow.rs` to establish overflow behavior and reachability for the entries that gate Phase 0A. | 19 entries added (`LIMIT-0056`..`LIMIT-0074`), 5 pass-1 entries upgraded to `Classified`, all six not-reached areas answered, and a silent-truncation register compiled. Method (a)'s weakness is the mirror of pass 1's: it finds only truncations whose author wrote a comment saying so. A truncation that is both unnamed and undocumented is still invisible to both passes; closing that would need an executable probe (oversized blocks, >128 channels, >32 rack stages) rather than a search. Still no measurement — every value remains read from source. | Pending `EVD` record for P00A-T004 |
