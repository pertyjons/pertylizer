[package]
name = "synth_core"
version = "0.1.0"
edition = "2024"
rust-version = "1.93"
description = "Core types, traits, and audio abstractions for modular synthesizer"
authors = ["Per"]
license = "MIT"

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
