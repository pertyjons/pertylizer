[package]
name = "synth_config"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Runtime configuration (pertylizer.toml) shared by the app and the visualizer"
authors.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
synth_osc_protocol = { path = "../synth_osc_protocol" }
serde = { workspace = true }
toml = { workspace = true }
